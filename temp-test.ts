import { fetchInstances, fetchGroups } from './services/evolutionApi'; (async () => { const instances = await fetchInstances(); if (instances.length > 0) { const firstInstance = instances[0]; console.log('Instance:', firstInstance.instanceName); const groups = await fetchGroups(firstInstance.instanceName); console.log('First 2 groups:'); groups.slice(0, 2).forEach((g, i) => { console.log(\nGroup ${i+1}: ${g.subject}); console.log(  Owner: ${g.owner}); console.log(  Size: ${g.size}); console.log(  Total participants: ${g.participants?.length || 0}); if(g.participants && g.participants.length > 0) { const myNumber = '209479043940588@lid'; const myInfo = g.participants.find(p => p.id === myNumber); if(myInfo) { console.log(  Your role in this group: ${myInfo.isSuperAdmin ? 'SUPER ADMIN' : myInfo.isAdmin ? 'ADMIN' : 'MEMBER'}); console.log(  User ID: ${myNumber}); } else { console.log('  You are not in this group'); } } }); } })().catch(console.error);
